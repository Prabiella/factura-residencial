import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'estado-de-cuenta-residencial',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
